% Simple CLP(R) program for testing
%
% Copyright 2016 National University of Singapore

% Note the first argument H representing global heap
% is mandatory.
dummy2(_H, X) :- X = 0.
dummy2(H, X + 1) :- X > 0, dummy2(H, X).
